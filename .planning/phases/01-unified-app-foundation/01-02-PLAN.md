---
phase: 01-unified-app-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - mafs-portal/src/components/Gateway.tsx
  - mafs-portal/src/components/MemberGateway.tsx
  - mafs-portal/src/components/VerificationScreen.tsx
  - mafs-portal/src/components/EnrollmentForm.tsx
  - mafs-portal/src/components/MemberPortal.tsx
  - mafs-portal/src/components/EmployeePortal.tsx
  - mafs-portal/src/components/CoverageModeling.tsx
  - mafs-portal/src/components/EligibilityCheck.tsx
  - mafs-portal/src/components/dashboard/Calculator.tsx
  - mafs-portal/src/components/dashboard/Dashboard.tsx
  - mafs-portal/src/components/dashboard/Projections.tsx
  - mafs-portal/src/components/dashboard/ClaimsTool.tsx
  - mafs-portal/src/components/dashboard/Reserves.tsx
autonomous: true
requirements:
  - P1-COMPONENTS

must_haves:
  truths:
    - "All 8 portal components exist in mafs-portal/src/components/"
    - "All 5 dashboard components exist in mafs-portal/src/components/dashboard/"
    - "Dashboard component imports use ../../ prefix (not ../) for types, constants, and services/engine"
    - "Portal components import from ../types, ../constants, ../services/engine (unchanged relative paths)"
    - "Sidebar.tsx is NOT copied (its navigation is replaced by EmployeePortal tab system)"
  artifacts:
    - path: "mafs-portal/src/components/Gateway.tsx"
      provides: "Role selector screen"
    - path: "mafs-portal/src/components/EmployeePortal.tsx"
      provides: "Staff portal with tab navigation"
    - path: "mafs-portal/src/components/dashboard/Calculator.tsx"
      provides: "Premium calculator from financial dashboard"
    - path: "mafs-portal/src/components/dashboard/ClaimsTool.tsx"
      provides: "Claim payout estimator (imports calculateClaimPayout)"
    - path: "mafs-portal/src/components/dashboard/Reserves.tsx"
      provides: "Reserve analysis (imports runMonteCarlo)"
  key_links:
    - from: "mafs-portal/src/components/dashboard/ClaimsTool.tsx"
      to: "mafs-portal/src/services/engine.ts"
      via: "import calculateClaimPayout"
      pattern: "import.*calculateClaimPayout.*from.*engine"
    - from: "mafs-portal/src/components/dashboard/Reserves.tsx"
      to: "mafs-portal/src/services/engine.ts"
      via: "import runMonteCarlo"
      pattern: "import.*runMonteCarlo.*from.*engine"
    - from: "mafs-portal/src/components/dashboard/Dashboard.tsx"
      to: "mafs-portal/src/services/engine.ts"
      via: "import generateProjections"
      pattern: "import.*generateProjections.*from.*engine"
---

<objective>
Copy all component files from both source apps into mafs-portal/. Portal components go to src/components/. Dashboard components go to src/components/dashboard/ with import path updates.

Purpose: Get all UI components into the unified app structure. Dashboard components need their relative import paths updated from `../` to `../../` because they move one directory level deeper (from `components/` to `components/dashboard/`).

Output: 13 component files in place, all importable, ready for integration wiring in Plan 03.
</objective>

<execution_context>
@/Users/ayoubazizi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ayoubazizi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-unified-app-foundation/01-CONTEXT.md
@.planning/phases/01-unified-app-foundation/01-RESEARCH.md
@.planning/phases/01-unified-app-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Import path rules for dashboard components after migration -->

Dashboard components currently import:
  import { X } from '../constants';
  import { Y } from '../services/engine';
  import { Z } from '../types';

After copying to src/components/dashboard/, paths become:
  import { X } from '../../constants';
  import { Y } from '../../services/engine';
  import { Z } from '../../types';

This applies to ALL 5 dashboard components. Each has exactly 2-3 imports with '../' prefix.

Portal components currently import:
  import { X } from '../types';
  import { Y } from '../services/engine';
  import { Z } from '../constants';

These paths remain UNCHANGED because portal components stay at src/components/ level.

Specific imports per dashboard component (from RESEARCH.md):
  Calculator.tsx: calculateMonthlyPremium from engine, COVERAGE_LIMITS from constants, CoverageTier from types
  Dashboard.tsx: DEFAULT_ASSUMPTIONS from constants, generateProjections from engine
  Projections.tsx: generateProjections from engine, DEFAULT_ASSUMPTIONS from constants, FinancialAssumptions from types
  ClaimsTool.tsx: CoverageTier from types, calculateClaimPayout from engine
  Reserves.tsx: runMonteCarlo from engine, DEFAULT_ASSUMPTIONS from constants
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy all 8 portal components verbatim</name>
  <files>
    mafs-portal/src/components/Gateway.tsx,
    mafs-portal/src/components/MemberGateway.tsx,
    mafs-portal/src/components/VerificationScreen.tsx,
    mafs-portal/src/components/EnrollmentForm.tsx,
    mafs-portal/src/components/MemberPortal.tsx,
    mafs-portal/src/components/EmployeePortal.tsx,
    mafs-portal/src/components/CoverageModeling.tsx,
    mafs-portal/src/components/EligibilityCheck.tsx
  </files>
  <read_first>
    mafs-member-portal/src/components/Gateway.tsx,
    mafs-member-portal/src/components/MemberGateway.tsx,
    mafs-member-portal/src/components/VerificationScreen.tsx,
    mafs-member-portal/src/components/EnrollmentForm.tsx,
    mafs-member-portal/src/components/MemberPortal.tsx,
    mafs-member-portal/src/components/EmployeePortal.tsx,
    mafs-member-portal/src/components/CoverageModeling.tsx,
    mafs-member-portal/src/components/EligibilityCheck.tsx
  </read_first>
  <action>
    Copy each of the 8 portal component files from `mafs-member-portal/src/components/` to `mafs-portal/src/components/`.

    Copy VERBATIM — no modifications. The relative import paths (`../types`, `../services/engine`, `../constants`, `../lib/supabase`) are already correct because portal components remain at the same directory depth in the unified app.

    Files to copy:
    1. `Gateway.tsx` — role selector (member vs employee)
    2. `MemberGateway.tsx` — member sign-in or explore mode
    3. `VerificationScreen.tsx` — KYC/verification form
    4. `EnrollmentForm.tsx` — membership application
    5. `MemberPortal.tsx` — tabbed member hub
    6. `EmployeePortal.tsx` — staff portal with tabs (will be modified in Plan 03 for dashboard integration)
    7. `CoverageModeling.tsx` — actuarial model display
    8. `EligibilityCheck.tsx` — eligibility criteria

    Do NOT copy any other files. Do NOT modify the files during copy.

    After copying, verify all 8 files exist: `ls mafs-portal/src/components/*.tsx | wc -l` should return 8.
  </action>
  <verify>
    <automated>cd /Users/ayoubazizi/Documents/Professional/Projects/MAFS/KEEP && ls mafs-portal/src/components/*.tsx | wc -l | grep -q 8 && echo "PASS: 8 portal components present"</automated>
  </verify>
  <acceptance_criteria>
    - mafs-portal/src/components/ contains exactly: Gateway.tsx, MemberGateway.tsx, VerificationScreen.tsx, EnrollmentForm.tsx, MemberPortal.tsx, EmployeePortal.tsx, CoverageModeling.tsx, EligibilityCheck.tsx
    - Each file is byte-identical to its source in mafs-member-portal/src/components/
    - No Sidebar.tsx from the dashboard is present
  </acceptance_criteria>
  <done>All 8 portal components present in mafs-portal/src/components/ with unchanged import paths</done>
</task>

<task type="auto">
  <name>Task 2: Copy 5 dashboard components with import path updates</name>
  <files>
    mafs-portal/src/components/dashboard/Calculator.tsx,
    mafs-portal/src/components/dashboard/Dashboard.tsx,
    mafs-portal/src/components/dashboard/Projections.tsx,
    mafs-portal/src/components/dashboard/ClaimsTool.tsx,
    mafs-portal/src/components/dashboard/Reserves.tsx
  </files>
  <read_first>
    mafs-financial-dashboard/components/Calculator.tsx,
    mafs-financial-dashboard/components/Dashboard.tsx,
    mafs-financial-dashboard/components/Projections.tsx,
    mafs-financial-dashboard/components/ClaimsTool.tsx,
    mafs-financial-dashboard/components/Reserves.tsx
  </read_first>
  <action>
    For each of the 5 dashboard components, copy from `mafs-financial-dashboard/components/` to `mafs-portal/src/components/dashboard/` and update ALL relative import paths.

    **Import path changes (apply to ALL 5 files):**
    - `'../constants'` becomes `'../../constants'`
    - `'../types'` becomes `'../../types'`
    - `'../services/engine'` becomes `'../../services/engine'`

    These are the ONLY changes. Do not modify any other code in the components.

    **Per-component verification after path update:**

    1. `Calculator.tsx`:
       - `import { calculateMonthlyPremium } from '../../services/engine';`
       - `import { COVERAGE_LIMITS } from '../../constants';`
       - `import { CoverageTier } from '../../types';`

    2. `Dashboard.tsx`:
       - `import { DEFAULT_ASSUMPTIONS } from '../../constants';`
       - `import { generateProjections } from '../../services/engine';`

    3. `Projections.tsx`:
       - `import { generateProjections } from '../../services/engine';`
       - `import { DEFAULT_ASSUMPTIONS } from '../../constants';`
       - `import { FinancialAssumptions } from '../../types';`

    4. `ClaimsTool.tsx`:
       - `import { CoverageTier } from '../../types';`
       - `import { calculateClaimPayout } from '../../services/engine';`

    5. `Reserves.tsx`:
       - `import { runMonteCarlo } from '../../services/engine';`
       - `import { DEFAULT_ASSUMPTIONS } from '../../constants';`

    Do NOT copy `Sidebar.tsx` — its navigation shell is replaced by the EmployeePortal tab system.

    After copying and updating, verify: `ls mafs-portal/src/components/dashboard/*.tsx | wc -l` should return 5.
    Also verify no remaining `'../constants'` or `'../types'` or `'../services/` (single ../) in dashboard files:
    `grep -r "'\.\./" mafs-portal/src/components/dashboard/ | grep -v "'\.\.\/\.\."` should return nothing.
  </action>
  <verify>
    <automated>cd /Users/ayoubazizi/Documents/Professional/Projects/MAFS/KEEP && ls mafs-portal/src/components/dashboard/*.tsx | wc -l | grep -q 5 && grep -rL "'../../" mafs-portal/src/components/dashboard/ | wc -l | grep -q 0 && echo "PASS: 5 dashboard components with correct paths"</automated>
  </verify>
  <acceptance_criteria>
    - mafs-portal/src/components/dashboard/ contains exactly: Calculator.tsx, Dashboard.tsx, Projections.tsx, ClaimsTool.tsx, Reserves.tsx
    - No Sidebar.tsx in dashboard/
    - Every import in dashboard components uses '../../' prefix (not '../')
    - grep "'../../services/engine'" mafs-portal/src/components/dashboard/ClaimsTool.tsx returns a match
    - grep "'../../services/engine'" mafs-portal/src/components/dashboard/Reserves.tsx returns a match
    - grep "calculateClaimPayout" mafs-portal/src/components/dashboard/ClaimsTool.tsx returns a match
    - grep "runMonteCarlo" mafs-portal/src/components/dashboard/Reserves.tsx returns a match
  </acceptance_criteria>
  <done>All 5 dashboard components present in mafs-portal/src/components/dashboard/ with correct ../../ import paths. No Sidebar.tsx copied.</done>
</task>

</tasks>

<verification>
1. `ls mafs-portal/src/components/*.tsx | wc -l` — must return 8
2. `ls mafs-portal/src/components/dashboard/*.tsx | wc -l` — must return 5
3. `grep -r "'\.\.\/services/engine'" mafs-portal/src/components/dashboard/` — all should use `../../services/engine`
4. `grep -r "'\.\.\/constants'" mafs-portal/src/components/dashboard/` — should return nothing (all should be `../../constants`)
5. `test ! -f mafs-portal/src/components/dashboard/Sidebar.tsx && echo "Sidebar correctly excluded"`
</verification>

<success_criteria>
- 8 portal component files in src/components/ (verbatim copies)
- 5 dashboard component files in src/components/dashboard/ (with updated import paths)
- No Sidebar.tsx anywhere in the unified app
- All dashboard imports use ../../ prefix for types, constants, services/engine
- Total: 13 component files ready for integration
</success_criteria>

<output>
After completion, create `.planning/phases/01-unified-app-foundation/01-02-SUMMARY.md`
</output>
