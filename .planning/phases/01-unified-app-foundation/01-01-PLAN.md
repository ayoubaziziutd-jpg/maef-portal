---
phase: 01-unified-app-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mafs-portal/package.json
  - mafs-portal/index.html
  - mafs-portal/vite.config.ts
  - mafs-portal/tsconfig.json
  - mafs-portal/src/main.tsx
  - mafs-portal/src/App.tsx
  - mafs-portal/src/types.ts
  - mafs-portal/src/constants.ts
  - mafs-portal/src/services/engine.ts
  - mafs-portal/src/lib/supabase.ts
autonomous: true
requirements:
  - P1-SCAFFOLD
  - P1-ENGINE

must_haves:
  truths:
    - "mafs-portal/ directory exists with all config files and the app scaffolds successfully"
    - "Unified engine.ts exports ALL functions from both source engines including calculateClaimPayout and runMonteCarlo"
    - "Unified types.ts includes YearProjection with investmentIncome field and ClaimCalculationResult with claimAmount, deductibleApplied, coinsuranceRate fields"
    - "npm install completes and npx tsc --noEmit passes with zero errors"
  artifacts:
    - path: "mafs-portal/package.json"
      provides: "Merged dependencies from both apps"
      contains: "lucide-react"
    - path: "mafs-portal/src/services/engine.ts"
      provides: "Unified calculation engine"
      exports: ["calculateMonthlyPremium", "calculateClaimPayout", "runMonteCarlo", "generateProjections", "soaModel", "casModel", "takafulModel", "hybridModel"]
    - path: "mafs-portal/src/types.ts"
      provides: "Unified domain types"
      contains: "investmentIncome"
    - path: "mafs-portal/vite.config.ts"
      provides: "Vite config with React plugin and @ alias"
  key_links:
    - from: "mafs-portal/src/services/engine.ts"
      to: "mafs-portal/src/types.ts"
      via: "type imports"
      pattern: "import.*from.*types"
    - from: "mafs-portal/src/services/engine.ts"
      to: "mafs-portal/src/constants.ts"
      via: "constant imports"
      pattern: "import.*from.*constants"
---

<objective>
Scaffold the mafs-portal/ directory and create the unified foundation files: package.json, config files, merged types, merged constants, merged engine, and bootstrap files.

Purpose: Establish the project skeleton and the single source-of-truth engine/types/constants that all subsequent component migrations depend on. Engine merge is the highest-risk item in this phase — TypeScript errors from missing exports or mismatched types cascade into every downstream component.

Output: A mafs-portal/ directory that installs, compiles (tsc --noEmit), and has all shared modules ready for components to import from.
</objective>

<execution_context>
@/Users/ayoubazizi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ayoubazizi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-unified-app-foundation/01-CONTEXT.md
@.planning/phases/01-unified-app-foundation/01-RESEARCH.md
@.planning/phases/01-unified-app-foundation/01-VALIDATION.md

<interfaces>
<!-- Source files to merge FROM. Executor must read these in full before writing. -->

Source engine (portal — structural base):
  mafs-member-portal/src/services/engine.ts
  Exports: determineAgeBracket, calculateMonthlyPremium, ficoLoadingFactor, soaModel, casModel, takafulModel, hybridModel, generateProjections, generateScenarioProjections
  MISSING: calculateClaimPayout, runMonteCarlo, generateGaussian (private helper)

Source engine (dashboard — functions to add):
  mafs-financial-dashboard/services/engine.ts
  Has: calculateClaimPayout, runMonteCarlo, generateGaussian (private)
  IMPORTANT: Dashboard's generateProjections includes investmentIncome in output — use THIS version, not portal's

Source types (portal — structural base):
  mafs-member-portal/src/types.ts
  Has: AppView, MemberTab, EmployeeTab, MemberRecord, ClaimRecord, MembershipRequest, AgeBracket, CoverageTier, etc.
  MISSING from YearProjection: investmentIncome
  MISSING from ClaimCalculationResult: claimAmount, deductibleApplied, coinsuranceRate

Source types (dashboard — fields to add):
  mafs-financial-dashboard/types.ts
  Has richer: YearProjection (with investmentIncome), ClaimCalculationResult (with claimAmount, deductibleApplied, coinsuranceRate), CoverageTier, FinancialAssumptions

Source constants:
  mafs-member-portal/src/constants.ts — USE THIS (portal is authoritative for business values)

Source supabase:
  mafs-member-portal/src/lib/supabase.ts — copy directly

Source App.tsx:
  mafs-member-portal/src/App.tsx — copy as basis for unified App.tsx

Source entry point:
  mafs-member-portal/src/index.tsx — copy as src/main.tsx (rename for Vite convention)

Dashboard index.html (for Tailwind config):
  mafs-financial-dashboard/index.html — Morocco color tokens and Inter font config
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mafs-portal/ scaffold with config files and bootstrap</name>
  <files>
    mafs-portal/package.json,
    mafs-portal/index.html,
    mafs-portal/vite.config.ts,
    mafs-portal/tsconfig.json,
    mafs-portal/src/main.tsx,
    mafs-portal/src/App.tsx,
    mafs-portal/src/lib/supabase.ts
  </files>
  <read_first>
    mafs-member-portal/package.json,
    mafs-financial-dashboard/package.json,
    mafs-financial-dashboard/index.html,
    mafs-financial-dashboard/vite.config.ts,
    mafs-financial-dashboard/tsconfig.json,
    mafs-member-portal/tsconfig.json,
    mafs-member-portal/src/index.tsx,
    mafs-member-portal/src/App.tsx,
    mafs-member-portal/src/lib/supabase.ts
  </read_first>
  <action>
    1. Create `mafs-portal/` directory structure: `src/`, `src/components/`, `src/components/dashboard/`, `src/services/`, `src/lib/`

    2. Create `mafs-portal/package.json` with merged dependencies:
       ```json
       {
         "name": "mafs-portal",
         "private": true,
         "version": "1.0.0",
         "type": "module",
         "scripts": {
           "dev": "vite",
           "build": "tsc && vite build",
           "preview": "vite preview"
         },
         "dependencies": {
           "@supabase/supabase-js": "^2.97.0",
           "lucide-react": "^0.574.0",
           "react": "^19.0.0",
           "react-dom": "^19.0.0",
           "recharts": "^3.7.0"
         },
         "devDependencies": {
           "@types/node": "^22.14.0",
           "@types/react": "^19.0.0",
           "@types/react-dom": "^19.0.0",
           "@vitejs/plugin-react": "^4.3.4",
           "typescript": "~5.7.2",
           "vite": "^6.0.0"
         }
       }
       ```
       Use lucide-react ^0.574.0 (higher version from dashboard). Include @types/node for vite.config.ts path resolution.

    3. Create `mafs-portal/index.html`:
       - Load Tailwind CDN: `<script src="https://cdn.tailwindcss.com"></script>`
       - Configure Morocco color tokens inline:
         ```javascript
         tailwind.config = {
           theme: { extend: {
             fontFamily: { sans: ['Inter', 'sans-serif'] },
             colors: { morocco: { red: '#C1272D', green: '#006233', green_light: '#008545', dark: '#1a1a1a' } }
           }}
         }
         ```
       - Load Inter font from Google Fonts CDN
       - Title: "MAFS Portal"
       - Mount point: `<div id="root"></div>`
       - Script: `<script type="module" src="/src/main.tsx"></script>`

    4. Create `mafs-portal/vite.config.ts`:
       ```typescript
       import path from 'path';
       import { defineConfig } from 'vite';
       import react from '@vitejs/plugin-react';

       export default defineConfig({
         server: { port: 5173 },
         plugins: [react()],
         resolve: {
           alias: { '@': path.resolve(__dirname, './src') }
         }
       });
       ```

    5. Create `mafs-portal/tsconfig.json`:
       ```json
       {
         "compilerOptions": {
           "target": "ESNext",
           "useDefineForClassFields": true,
           "lib": ["DOM", "DOM.Iterable", "ESNext"],
           "allowJs": false,
           "skipLibCheck": true,
           "esModuleInterop": true,
           "allowSyntheticDefaultImports": true,
           "strict": true,
           "forceConsistentCasingInFileNames": true,
           "module": "ESNext",
           "moduleResolution": "bundler",
           "resolveJsonModule": true,
           "isolatedModules": true,
           "noEmit": true,
           "jsx": "react-jsx",
           "baseUrl": ".",
           "paths": { "@/*": ["./src/*"] }
         },
         "include": ["src"]
       }
       ```
       Use moduleResolution "bundler" (Vite 6 preference, matches dashboard).

    6. Copy `mafs-member-portal/src/index.tsx` to `mafs-portal/src/main.tsx` — keep the ReactDOM.createRoot logic verbatim. Only change: ensure it mounts on `document.getElementById('root')`.

    7. Copy `mafs-member-portal/src/App.tsx` to `mafs-portal/src/App.tsx` verbatim. All component imports will be resolved in Plan 02 when components are copied. For now the file just needs to exist so tsc can parse the structure.

    8. Copy `mafs-member-portal/src/lib/supabase.ts` to `mafs-portal/src/lib/supabase.ts` verbatim.

    9. Run `cd mafs-portal && npm install --cache /tmp/npm-cache-mafs` to install dependencies. Use --cache flag to avoid permission issues with system npm cache.
  </action>
  <verify>
    <automated>test -f mafs-portal/package.json && test -f mafs-portal/vite.config.ts && test -f mafs-portal/tsconfig.json && test -f mafs-portal/index.html && test -f mafs-portal/src/main.tsx && test -f mafs-portal/src/App.tsx && test -f mafs-portal/src/lib/supabase.ts && test -d mafs-portal/node_modules && echo "PASS: scaffold complete"</automated>
  </verify>
  <acceptance_criteria>
    - mafs-portal/package.json contains "lucide-react": "^0.574.0" and "@supabase/supabase-js": "^2.97.0"
    - mafs-portal/index.html contains "cdn.tailwindcss.com" and "morocco" and "#C1272D" and "Inter"
    - mafs-portal/vite.config.ts contains "port: 5173" and "@vitejs/plugin-react"
    - mafs-portal/tsconfig.json contains "moduleResolution": "bundler" and "react-jsx"
    - mafs-portal/src/main.tsx contains "createRoot"
    - mafs-portal/node_modules/ directory exists (npm install succeeded)
  </acceptance_criteria>
  <done>mafs-portal/ directory exists with all config files, bootstrap files, supabase client, and node_modules installed</done>
</task>

<task type="auto">
  <name>Task 2: Create unified types.ts, constants.ts, and merged engine.ts</name>
  <files>
    mafs-portal/src/types.ts,
    mafs-portal/src/constants.ts,
    mafs-portal/src/services/engine.ts
  </files>
  <read_first>
    mafs-member-portal/src/types.ts,
    mafs-financial-dashboard/types.ts,
    mafs-member-portal/src/constants.ts,
    mafs-financial-dashboard/constants.ts,
    mafs-member-portal/src/services/engine.ts,
    mafs-financial-dashboard/services/engine.ts
  </read_first>
  <action>
    **types.ts — merge strategy:**

    1. Start with the FULL contents of `mafs-member-portal/src/types.ts` (it has AppView, MemberTab, EmployeeTab, MemberRecord, ClaimRecord, MembershipRequest, ScenarioAssumptions, DiscountConfig, and more).

    2. Find the `YearProjection` interface. ADD the `investmentIncome: number;` field (present in dashboard types, missing in portal). The final interface MUST be:
       ```typescript
       export interface YearProjection {
         year: number;
         members: number;
         premiumRevenue: number;
         expectedClaims: number;
         adminExpenses: number;
         investmentIncome: number;
         netIncome: number;
         reserves: number;
         reserveRatio: number;
         combinedRatio: number;
       }
       ```

    3. Find the `ClaimCalculationResult` interface. ADD missing fields from the dashboard version. The final interface MUST be:
       ```typescript
       export interface ClaimCalculationResult {
         claimAmount: number;
         fundPays: number;
         memberPays: number;
         deductibleApplied: number;
         coinsuranceRate: number;
         withinLimits: boolean;
         notes: string[];
       }
       ```

    4. If the dashboard types.ts has `FinancialAssumptions` interface that is NOT in the portal types — add it. Dashboard components (Dashboard.tsx, Projections.tsx, Reserves.tsx) import this type.

    5. Verify all types referenced by dashboard components exist: `CoverageTier`, `AgeBracket`, `FinancialAssumptions`, `YearProjection`, `ClaimCalculationResult`.

    **constants.ts — merge strategy:**

    1. Copy `mafs-member-portal/src/constants.ts` verbatim. Per user decision, the portal is the authoritative source for business values (PREMIUM_RATES, COVERAGE_LIMITS, etc.).

    2. Check if `DEFAULT_ASSUMPTIONS` exists in the portal constants. If not, add it from the dashboard constants — the Dashboard, Projections, and Reserves components all import `DEFAULT_ASSUMPTIONS`.

    3. Check if `COVERAGE_LIMITS` exists in the portal constants. If not, add it from the dashboard — Calculator.tsx imports it.

    4. Check if `FAMILY_MULTIPLIERS` exists. If not, add from whichever source has it.

    **engine.ts — merge strategy (HIGHEST RISK):**

    1. Start with the FULL contents of `mafs-member-portal/src/services/engine.ts`. This contains:
       - `determineAgeBracket`
       - `calculateMonthlyPremium` (6-param version with optional community/annual flags)
       - `ficoLoadingFactor`
       - `soaModel`, `casModel`, `takafulModel`, `hybridModel` (actuarial models)
       - `generateProjections`
       - `generateScenarioProjections`

    2. From `mafs-financial-dashboard/services/engine.ts`, copy and append these functions that are MISSING from the portal engine:
       - `generateGaussian()` — private helper (not exported), required by `runMonteCarlo`. Uses Box-Muller transform.
       - `calculateClaimPayout(claimAmount, tier, annualClaimsYtd?)` — exported, required by ClaimsTool.tsx
       - `runMonteCarlo(members, frequency, avgClaim, simulations?)` — exported, required by Reserves.tsx

    3. CRITICAL: Replace the portal's `generateProjections` function with the DASHBOARD's version. Why: The dashboard version computes and includes `investmentIncome` in the returned YearProjection objects. The portal version does NOT. Since YearProjection now includes `investmentIncome`, the function must produce it. Read both implementations carefully and use the dashboard's.

    4. Update all import paths in engine.ts: types and constants should import from `'../types'` and `'../constants'` (one level up from `src/services/`).

    5. Ensure engine.ts imports match the unified types: `YearProjection`, `ClaimCalculationResult`, `FinancialAssumptions`, `CoverageTier`, `AgeBracket`, `ScenarioAssumptions`.

    After creating all three files, run: `cd mafs-portal && npx tsc --noEmit 2>&1 | head -30`

    NOTE: tsc will show errors for missing component files (App.tsx imports components that don't exist yet). That's expected. The important thing is that types.ts, constants.ts, and engine.ts have ZERO errors among themselves. Filter the output to check only these files.
  </action>
  <verify>
    <automated>cd /Users/ayoubazizi/Documents/Professional/Projects/MAFS/KEEP/mafs-portal && grep "investmentIncome" src/types.ts && grep "claimAmount" src/types.ts && grep "deductibleApplied" src/types.ts && grep "coinsuranceRate" src/types.ts && grep "export.*calculateClaimPayout" src/services/engine.ts && grep "export.*runMonteCarlo" src/services/engine.ts && grep "generateGaussian" src/services/engine.ts && grep "export.*soaModel" src/services/engine.ts && grep "export.*generateProjections" src/services/engine.ts && echo "PASS: all exports and types present"</automated>
  </verify>
  <acceptance_criteria>
    - mafs-portal/src/types.ts contains "investmentIncome: number"
    - mafs-portal/src/types.ts contains "claimAmount: number" and "deductibleApplied: number" and "coinsuranceRate: number"
    - mafs-portal/src/types.ts contains "FinancialAssumptions" interface
    - mafs-portal/src/services/engine.ts contains "export const calculateClaimPayout"
    - mafs-portal/src/services/engine.ts contains "export const runMonteCarlo"
    - mafs-portal/src/services/engine.ts contains "function generateGaussian" (private helper)
    - mafs-portal/src/services/engine.ts contains "export const soaModel" and "export const casModel" and "export const takafulModel" and "export const hybridModel"
    - mafs-portal/src/services/engine.ts contains "export const generateProjections" and the implementation includes "investmentIncome" in the returned object
    - mafs-portal/src/services/engine.ts contains "export const calculateMonthlyPremium" with 6 parameters (age, tier, hasSpouse, childrenCount, communityMember?, annualPayment?)
    - mafs-portal/src/constants.ts contains "PREMIUM_RATES" and "COVERAGE_LIMITS" and "DEFAULT_ASSUMPTIONS"
  </acceptance_criteria>
  <done>Unified types.ts has all enriched interfaces, constants.ts has all required constants, engine.ts exports all functions from both source engines with the dashboard's generateProjections implementation</done>
</task>

</tasks>

<verification>
1. `test -f mafs-portal/package.json && test -f mafs-portal/vite.config.ts && echo "scaffold exists"`
2. `grep "export.*calculateClaimPayout" mafs-portal/src/services/engine.ts` — must match
3. `grep "export.*runMonteCarlo" mafs-portal/src/services/engine.ts` — must match
4. `grep "investmentIncome" mafs-portal/src/types.ts` — must match
5. `grep "DEFAULT_ASSUMPTIONS" mafs-portal/src/constants.ts` — must match
6. `cd mafs-portal && npx tsc --noEmit 2>&1 | grep -c "error TS"` — errors should only be from missing component files, not from types/engine/constants
</verification>

<success_criteria>
- mafs-portal/ directory exists with package.json, index.html, vite.config.ts, tsconfig.json
- node_modules installed successfully
- src/types.ts, src/constants.ts, src/services/engine.ts all exist with complete merged content
- Engine exports: calculateMonthlyPremium, calculateClaimPayout, runMonteCarlo, generateProjections, soaModel, casModel, takafulModel, hybridModel, determineAgeBracket, ficoLoadingFactor, generateScenarioProjections
- YearProjection includes investmentIncome
- ClaimCalculationResult includes claimAmount, deductibleApplied, coinsuranceRate
</success_criteria>

<output>
After completion, create `.planning/phases/01-unified-app-foundation/01-01-SUMMARY.md`
</output>
